runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: stdio
  commandFunction: |
    () => ({
      command: 'python',
      args: ['-m', 'tcmb_mcp_server.server']
    })
